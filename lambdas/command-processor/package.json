{
  "dependencies": {
    "@aws-sdk/client-sqs": "*",
    "aws-lambda": "^1.0.7",
    "axios": "^1.13.2",
    "esbuild": "^0.25.9",
    "nhs-notify-sms-nudge-utils": "*",
    "zod": "^3.25.75"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.2",
    "@types/aws-lambda": "^8.10.148",
    "@types/jest": "^29.5.14",
    "@types/node": "^25.0.3",
    "eslint-plugin-react-hooks": "^7.0.1",
    "jest": "^29.7.0",
    "jest-mock-extended": "^3.0.7",
    "typescript": "^5.8.2"
  },
  "name": "nhs-notify-sms-nudge-command-processor",
  "private": true,
  "scripts": {
    "lambda-build": "rm -rf dist && npx esbuild --bundle --minify --sourcemap --target=es2020 --platform=node --loader:.node=file --entry-names=[name] --outdir=dist src/index.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:unit": "jest",
    "typecheck": "tsc --noEmit"
  },
  "version": "0.0.1"
}
